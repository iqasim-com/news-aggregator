import {PreferenceTypesProps} from "../components/types.ts";

/**
 * Represents the preference types available in the system.
 */
export const PREFERENCE_TYPES: PreferenceTypesProps = {
  AUTHORS: "authors",
  CATEGORIES: "categories",
  SOURCES: "sources",
}
